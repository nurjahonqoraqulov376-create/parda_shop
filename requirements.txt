# --- Asosiy ---
Django>=5.0,<6.0
Pillow>=10.0
django-environ>=0.11

# --- Ishlab chiqarish (Railway va boshqa serverlar) ---
# WSGI server: Django'ning `runserver` i faqat ishlab chiqish uchun.
gunicorn>=22.0
# Statik fayllarni (css/js) Django o'zi beradi — Railway'da nginx yo'q.
whitenoise>=6.6
# PostgreSQL drayveri. Railway'da SQLite ishlatib bo'lmaydi: fayl tizimi
# har qayta joylashda tozalanadi va baza yo'qoladi.
psycopg[binary]>=3.1
